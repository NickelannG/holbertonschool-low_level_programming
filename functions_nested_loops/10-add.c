#include "main.h"
/**
 * add - adds two integers and returns the result
 *
 * @a: First integer
 * @b: Second integer
 * Return: the sum of integer a and b
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
